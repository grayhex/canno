# Canno Quest

MVP для офлайн-квестов с уникальной ссылкой участника, этапами, паролями и таймерами.

## Запуск

```bash
export CANNO_ADMIN_USER=admin
export CANNO_ADMIN_PASSWORD='change-me'
python3 app.py
```

Откроется на `http://localhost:8000`.

## Авторизация админки

- Доступ к `/admin` только после входа через `/admin/login`.
- По умолчанию используется `CANNO_ADMIN_USER` и пароль из `CANNO_ADMIN_PASSWORD`.
- Для production лучше использовать `CANNO_ADMIN_PASSWORD_HASH` в формате `pbkdf2_sha256$ITERATIONS$SALT$HASH`.
- Включен rate-limit на попытки логина с одного IP (5 попыток за 5 минут).

## Что реализовано
- Участник проходит квест по уникальному `/play/<token>`.
- Количество этапов произвольное.
- Строгая проверка пароля.
- Таймер на квест и на этап.
- При истечении времени блокировка до следующего дня по `Europe/Moscow`.
- Админка `/admin`: авторизация, генерация ссылок и просмотр прогресса.

## Следующие шаги
- Вынести хранение сессий и ограничений логина из памяти процесса.
- CRUD для квестов/этапов и включение/выключение квеста.
- Docker Compose + Postgres.
